# Remote Era File Configuration
# ============================

# Required: Base URL for remote era files
ERA_BASE_URL=https://era-files.com

# Optional: Directory for temporary downloads (default: system temp)
ERA_DOWNLOAD_DIR=./temp_era_files

# Optional: Whether to delete files after processing (default: true)
ERA_CLEANUP_AFTER_PROCESS=true

# Optional: Maximum download retries (default: 3)
ERA_MAX_RETRIES=3

# Optional: Maximum concurrent downloads (future use)
ERA_MAX_CONCURRENT_DOWNLOADS=2

# ClickHouse Configuration
# ========================

# Required: ClickHouse host (ClickHouse Cloud endpoint)
CLICKHOUSE_HOST=your-cluster.clickhouse.cloud

# Required: ClickHouse port (8443 for ClickHouse Cloud with TLS)
CLICKHOUSE_PORT=8443

# Required: ClickHouse username
CLICKHOUSE_USER=default

# Required: ClickHouse password
CLICKHOUSE_PASSWORD=your-password-here

# Optional: ClickHouse database name (default: beacon_chain)
CLICKHOUSE_DATABASE=beacon_chain

# Optional: Use TLS/SSL connection (default: true for ClickHouse Cloud)
CLICKHOUSE_SECURE=true

# Optional: Batch size for inserts (default: 1000)
CLICKHOUSE_BATCH_SIZE=1000